# Mapping
This is a simple Python project using the Folium library to create an interactive map. The map is centered on the Azores (Latitude 38, Longitude -27) and saved as an HTML file. This allows users to view the map in a browser with zoom and pan capabilities using Leaflet.js.
